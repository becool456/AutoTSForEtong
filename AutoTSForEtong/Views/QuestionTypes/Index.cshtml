@model IEnumerable<AutoTSForETongModel.QuestionType>

@{
    ViewBag.Title = "Index";
}


<p>
    @Html.ActionLink("添加题型", "Create")
</p>
<table class="table">
    <tr>
        <th>
            @Html.DisplayNameFor(model => model.TypeName)
        </th>
        <th></th>
    </tr>

@foreach (var item in Model) {
    <tr>
        <td>
            @Html.DisplayFor(modelItem => item.TypeName)
        </td>
        @*<td>
            @Html.ActionLink("编辑", "Edit", new { id=item.TypeID }) |
            @Html.ActionLink("", "Delete", new { id=item.TypeID })
        </td>*@
    </tr>
}

</table>
